import { AlwaysfocusDirective } from './alwaysfocus.directive';

describe('AlwaysfocusDirective', () => {
  it('should create an instance', () => {
    const directive = new AlwaysfocusDirective();
    expect(directive).toBeTruthy();
  });
});
